# Quartz 博客使用说明

## 当前安全策略

- Quartz 只生成带有 `publish: true` 属性的 Markdown 页面。
- `private`、`templates`、`.obsidian`、`.trash` 和 `.git` 目录会被忽略。
- 当前 `content` 是独立的示例目录，尚未连接坚果云中的任何 Obsidian 仓库。
- 不建议把整个私人仓库直接链接为 `content`：未引用的图片等非 Markdown 附件仍可能作为静态资源进入构建产物。

## 推荐的坚果云目录结构

在最终选定的 Obsidian 仓库里建立一个专用公开目录：

```text
公开博客/
├── index.md
├── 文章/
└── assets/
```

只把 `公开博客` 目录链接到 Quartz 的 `content`。这样，坚果云继续负责手机与电脑同步，Quartz 只读取公开目录。

## 常用操作

在项目目录中执行本地预览：

```sh
node quartz/bootstrap-cli.mjs build --serve
```

默认访问地址是 `http://localhost:8080`。

修改文章后，预览会自动更新。准备上线前，需要把 `quartz.config.yaml` 中的 `baseUrl` 从 `example.com` 改成最终域名。
